<h3>Defined Under Namespace</h3>
<p class="children">
  <% @inner.each do |name, list| %>
    <% if list.size > 0 %>
      <strong class="<%= name %>"><%= name.to_s.capitalize %>:</strong> <%= list.map {|child| linkify(child, child.name) }.join(", ") %>
    <% end %>
  <% end %>
</p>
